/**
 * Describes the border colors of the elements in various states.
 */
export const ElementColors: { [key: string]: string } = {
	default: "#000000",
	detail: "#0000FF",
	select: "#FF9037"
};

Object.freeze(ElementColors);
